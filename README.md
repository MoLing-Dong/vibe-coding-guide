# Vibe Coding Guide

基于 VitePress 的中英文教程站，用来指导开发者如何通过 AI 编程助手进行 vibe coding：明确目标、提供上下文、拆解任务、验证结果，并把原型推进到可交付状态。

## 本地开发

```bash
npm install
npm run dev
```

默认访问 `http://localhost:5173`。

英文入口：`http://localhost:5173/en/`。

## 构建

```bash
npm run build
npm run preview
```
